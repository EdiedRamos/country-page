import { Content, Region, SortBy } from "@/components";

export const ContentLayer = () => {
  return (
    <div className="bg-cc-black">
      <Content />
      <SortBy />
      <Region />
    </div>
  );
};
